name: Release

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - stage
      - dev
    # paths:
    #   - "**/*.tf"
jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name:
        run: |
          git describe --tags
          export APPVERSION=$(git describe --tags)
          echo $APPVERSION
      - name: env
        run: env
